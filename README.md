hjartesorg
==========

## Getting started

Install Gulp globally
``` npm install -g gulp ```

Install local node modules
``` npm install ```

Run the app
``` gulp ```

The app should now be running on localhost:3000!
